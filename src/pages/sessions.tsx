import Layout from '@/components/Layout';
import { NextPage } from 'next';
import React from 'react';

type Props = {};

const Sessions: NextPage = (_: Props) => {
  return <Layout>Sessions</Layout>;
};

export default Sessions;
